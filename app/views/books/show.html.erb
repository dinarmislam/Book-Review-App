<div class="row">

	<div class="col-md-4">

		

	</div>

	<div class="col-md-8">

		<div class="book-info">

			<h2 class="book-title"><%= @book.title %></h2>
			<h3 class="book-author"><span>From</span> <%= @book.author %></h3>
			<% if @book.category_id? %><h4 class="book-category"><span>Category</span> <%= link_to @book.category.name, books_path(category: @book.category.name) %></h4><% end %>
			<p class="book-desc"><%= @book.description %></p>

		</div>
	</div>


</div>



<div class="links btn-group">

	<%= link_to "Back", root_path, class: "btn btn-custom" %>

	<% if user_signed_in? %>

		<% if @book.user_id == current_user.id %>

			<%= link_to "Edit", edit_book_path(@book), class: "btn btn-custom" %>
			<%= link_to "Delete", book_path(@book), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-custom" %>

		<% end %>

	<% end %>	

</div>	


